# kithara-audio-tests

Integration tests for the public `kithara::audio` facade live in [`tests`](tests).
The package owns regular and allocation-heavy audio binaries so edits in other
domains can reuse both compiled artifacts.
