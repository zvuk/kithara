# kithara-play-tests

Integration tests for the public `kithara::play` facade live in [`tests`](tests).
Regular, heavy, stress, device, and network contracts use separate binaries so
each execution surface keeps an independent cached artifact.
