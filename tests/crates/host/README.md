# kithara-host-tests

Host mixing contracts that need a rendered session live in [`tests`](tests):
the weighted player sum, the limiter, session ducking, and the post-limiter
mix tap. Host ownership contracts that need no rendering are crate tests in
`kithara-host`.
