# kithara-assets-tests

Integration tests for the public `kithara::assets` facade live in
[`tests`](tests). The package keeps storage, persistence, processing, and
eviction contracts in one domain binary that unrelated test changes can reuse.
