# kithara-broadcast-tests

Integration tests for the public `kithara::broadcast` facade live in
[`tests`](tests). Live packaging and origin contracts compile only when the
broadcast lane selects this package feature.
