# kithara-app-tests

Integration tests for the non-UI `kithara-app` analysis services live in
[`tests`](tests). The package keeps beat and waveform analyzer contracts in one
test binary that unrelated domains can leave cached.
