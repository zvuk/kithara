[package]
name = "kithara-app-tests"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
publish = false
autotests = false

[package.metadata.cargo-machete]
ignored = ["kithara-test-utils"]

[features]
default = ["client-reqwest", "tls-rustls"]
client-reqwest = ["kithara/client-reqwest"]
client-wreq = ["kithara/client-wreq"]
tls-rustls = ["kithara/tls-rustls"]
tls-native = ["kithara/tls-native"]
flash = ["kithara/flash", "kithara-integration-tests/flash"]
no-block = ["kithara/no-block", "kithara-integration-tests/no-block"]

[dependencies]
kithara = { workspace = true, features = [
    "usdt",
    "analysis",
    "assets",
    "bufpool",
    "platform",
    "play",
] }
kithara-app = { workspace = true, features = ["analysis-waveform", "beat-nn-small", "lib-only"] }
kithara-integration-tests = { workspace = true, features = ["all"] }
kithara-test-dylib = { workspace = true }

kithara-test-utils = { workspace = true, features = ["usdt", "hang", "mock"] }

num-traits = { workspace = true }
url = { workspace = true }

[lints]
workspace = true

[[test]]
name = "app"
path = "tests/suite.rs"

# Its own binary: the counting allocator it installs would otherwise measure
# every other test in the `app` suite alongside the construction under test.
[[test]]
name = "app_memory"
path = "tests/memory_suite.rs"
