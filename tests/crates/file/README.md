# kithara-file-tests

Integration tests for the public `kithara::file` facade live in [`tests`](tests).
Regular and real-media stress cases have separate binaries so unrelated domain
changes can reuse their compiled artifacts.
