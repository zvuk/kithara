# kithara-ffi-web-tests

Browser and Selenium integration tests for the Kithara web FFI live in
[`tests`](tests). The package keeps browser-specific codegen outside the native
audio and playback test binaries.
