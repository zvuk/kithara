{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "repo=${CLAUDE_PROJECT_DIR:-$PWD}; until [ -x \"$repo/tools/agent-hook/run\" ]; do [ \"$repo\" != / ] || { printf 'error: cannot find tools/agent-hook/run\\n' >&2; exit 1; }; repo=${repo%/*}; [ -n \"$repo\" ] || repo=/; done; exec \"$repo/tools/agent-hook/run\" pre-bash"
          }
        ]
      }
    ]
  }
}
