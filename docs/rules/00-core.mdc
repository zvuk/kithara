---
description: Core repo contract for Kithara agents.
alwaysApply: true
---

# Entry Rule

- Follow the canonical repo guidance in `AGENTS.md`.
- Use `docs/workflows/rust-ai.md` only when the task needs planning, split execution, or handoff structure.
- Keep tool-specific files thin and do not invent Claude-only repo rules or task packet shapes.
