{
  "attribution": {
    "commit": "",
    "pr": ""
  },
  "enabledPlugins": {
    "ralph-loop@claude-plugins-official": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "command": "command -v just >/dev/null 2>&1 || { printf 'warning: just is unavailable; skipping Kithara agent hook\\n' >&2; exit 0; }; exec just agent-hook",
            "type": "command"
          }
        ],
        "matcher": "Write|Edit|MultiEdit"
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "command": "command -v just >/dev/null 2>&1 || { printf 'warning: just is unavailable; skipping Kithara agent hook\\n' >&2; exit 0; }; exec just agent-hook",
            "type": "command"
          }
        ],
        "matcher": "Bash"
      }
    ]
  }
}
