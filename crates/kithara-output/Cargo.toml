[package]
name = "kithara-output"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Master-output and offline-rendering protocols for Kithara."
keywords = ["audio", "output", "rendering"]
categories = ["multimedia::audio"]

[dependencies]
kithara-platform = { workspace = true }
kithara-signal = { workspace = true }

bon = { workspace = true }
thiserror = { workspace = true }

[target.'cfg(all(not(target_arch = "wasm32"), not(target_os = "android")))'.dependencies]
kithara-workspace-hack = { version = "0.0.1-alpha4", path = "../kithara-workspace-hack" }

[dev-dependencies]
kithara-test-utils = { workspace = true, features = ["client-reqwest", "tls-rustls"] }

[lints]
workspace = true
