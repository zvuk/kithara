[package]
name = "kithara-sync"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Musical synchronization execution contracts for Kithara."
keywords = ["audio", "beatgrid", "dj", "synchronization"]
categories = ["multimedia::audio"]

[dependencies]
kithara-warp = { workspace = true }

[target.'cfg(all(not(target_arch = "wasm32"), not(target_os = "android")))'.dependencies]
kithara-workspace-hack = { version = "0.0.1-alpha4", path = "../kithara-workspace-hack" }

[lints]
workspace = true
