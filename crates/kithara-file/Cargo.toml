[package]
name = "kithara-file"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
kithara-core = { workspace = true }
kithara-net = { workspace = true }
bytes = { workspace = true }
futures = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tokio-stream = { workspace = true }
url = { workspace = true }
async-stream = { workspace = true }

# Required dependency for caching.
# The file source can persist downloaded bytes for offline reuse.
kithara-cache = { workspace = true }

[dev-dependencies]
# Keep dev deps workspace-first; add here only after adding to root [workspace.dependencies].
axum = { workspace = true }
