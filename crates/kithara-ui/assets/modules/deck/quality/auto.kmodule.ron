(
    schema: "kithara.module",
    version: 1,
    id: "deck-quality-auto",
    parameters: ["deck"],
    root: Pressable(
        id: "pick",
        press: Command(id: "deck.stream.select_variant", with: { "deck": "$deck", "variant": "auto" }),
        child: Row(
            size: (w: Fill, h: Fixed(26.0)),
            pad_x: 10.0,
            gap: 8.0,
            active: Model(id: "deck.stream.variant_active", with: { "deck": "$deck", "variant": "auto" }),
            active_background: BgSelect,
            children: [
                Row(
                    id: "dot",
                    size: (w: Fixed(6.0), h: Fixed(6.0)),
                    gap: 0.0,
                    background: Line,
                    active: Model(id: "deck.stream.variant_active", with: { "deck": "$deck", "variant": "auto" }),
                    active_background: Accent,
                    children: [],
                ),
                Text(
                    id: "label",
                    size: (w: Fill, h: Fixed(13.0)),
                    style: Mono,
                    active_color: Text,
                    align: Start,
                    label: "@deck.stream.auto_label",
                    active: Model(id: "deck.stream.variant_active", with: { "deck": "$deck", "variant": "auto" }),
                ),
                Text(id: "sub", style: MicroLabel, label: "@deck.stream.auto_sub"),
            ],
        ),
    ),
)
