#![cfg(all(feature = "capture", feature = "masonry"))]

//! The embedded application host, mounted on a document and driven the way a
//! person drives it, through the public API.

mod picked;
mod scenario;
mod ui;
